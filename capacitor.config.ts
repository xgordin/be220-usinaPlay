import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'be220-unisaPlay',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
